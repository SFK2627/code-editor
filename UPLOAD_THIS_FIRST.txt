Upload index.html to GitHub root. This index.html already contains the CSS, app script, and Firebase config, so the page will not become plain/un-styled if style.css or script.js fails to load. You may also upload the other files as backup/editable copies.

For Firebase Rules, do NOT open/upload firestore.rules as the homepage. Copy its contents only into Firebase Console > Firestore Database > Rules > Publish.
